# 2026-07-21

Direction day — no code. Decided the **v2 repositioning**: BrainCue widens from
interview copilot to **universal ambient conversational companion** — it sits in
your real conversations and activities (interviews on either side, meetings,
tutoring, ambient companionship while working/gaming) and contributes through
the overlay or voice. Interviews become one mode of a shared engine.

**Why this framing (and not "voice mode clone"):** generic voice chat is
ChatGPT/Gemini turf. BrainCue's structural moat is what a cloud chat app can't
do — loopback presence inside real calls, a capture-invisible overlay, local
grounding in the user's corpus, BYO key. Positioning: "the AI in the room with
you"; voice is a surface, not the product.

**Key insight from the code audit:** the universal primitives largely exist —
realtime STT, question classifier (= a reactive trigger policy), RAG, streaming
answers, TTS, and a full two-way voice loop in sparring/mock. The pivot is
engine extraction + repositioning, not a rebuild.

## Docs written

- `00-VISION.md` (new) — north star, moat, mode catalog, principles, brand.
- `01-PRD.md` — **rewritten as the v2 spec** (v1 PRD preserved in git history):
  domain model (Profile / Context Pack / Session / Mode / Memory), engine spec
  (sources → STT → trigger policy → grounding → generation → surfaces), per-mode
  requirements, migration + parity requirements.
- `10-ROADMAP.md` (new) — P1 v2.0 engine extraction + rebrand → P2 v2.1 Meeting
  Copilot + Interviewer Assist → P3 v2.2 Realtime speech + Tutor → P4 v3.0
  Memory + Companion. New dev rules: engine-first (modes are config, never
  forks), parity gate (unit suite + privacy hard test at phase boundaries),
  Labs flags, lossless migrations.
- `09-MVP-PLAN.md` — marked historical (v1 fully shipped).

## Decisions

- Keep the **BrainCue** name and appId; retire the "interview copilot"
  descriptor. "Cue Card" stays.
- Interview mode is the regression gate — no refactor may degrade it.
- Companion/game-buddy is deliberately **last** (needs memory, interjection
  policy, realtime voice, cost governance).
- Mock/sparring migrate onto the engine's dialogue loop **in Phase 3** (with
  Tutor), not during the Phase 1 extraction.

## Addendum (same day) — README, multi-provider, navigation redesign

- **README.md rewritten** for the v2 positioning: "The AI that's in the room
  with you", mode-catalog table up top, interview media kept but framed as "the
  first mode" (assets to be refreshed later), BYO-AI wording. Repo URL, badges,
  download/sysreq/release sections intact.
- **Multi-provider support specced** (user decision: not OpenAI-only later):
  vision principle 1 now "provider-agnostic"; PRD gains **§6.7 Provider layer**
  (capability interfaces `chat`/`embeddings`/`stt`/`speech`/`vision`, per-provider
  keys, mix-and-match by capability, graceful degradation, embedding re-index
  caveat) + a lock-in risk row; roadmap: seam cut in **1.2**, second provider
  (chat+vision) as new **2.3**, realtime STT/speech peers + local models
  deferred until the layer is proven.
- **`11-UX-NAVIGATION.md` (new)** — the mode-first layout redesign. Principle:
  *chrome scales with structure, not modes*. Sidebar collapses 7 → 4
  (Home / Library / Reports / Settings); modes become launcher cards on Home;
  Profiles+Jobs merge into Library (Profiles · Context Packs tabs); **Tailor
  Resume becomes a pack action** rather than a nav item; `InterviewPage` splits
  into SetupSheet + shared SessionView with a sidebar Live pill; all old routes
  redirect (tray deep links + tour survive); overlay explicitly untouched
  (parity gate). Execution steps folded into roadmap milestone 1.3.

## Open

- Final tagline wording; when `productName` copy changes (Phase 1.4 vs v2.0
  release).
- "Context Pack" naming — alternatives considered: Space, Topic. Revisit before
  the 1.1 schema PR since it names a table.
- Salience-classifier design for the proactive policy (Phase 2.1) — needs a
  prototype before committing to prompt-vs-heuristic split.
- Home vs Start naming; whether Practice stays its own Home card after Tutor
  absorbs the drill loop (see 11-UX-NAVIGATION.md §7).
